INSTALLATION

1. Install node.js [http://nodejs.org/]
2. Clone the project from github.
3. Open a cmd window in the server folder of this project, then run "npm install [modulename]".
The modules are:
	- express
	- socket.io